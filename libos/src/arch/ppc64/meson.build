libos_sources_arch = files(
    'fs_proc/info.c',
    'return_from_syscall_emulation.S',
    'rt_sigreturn_wrapper.S',
    'libos_context.c',
    'libos_elf_entry.S',
    'libos_table.c',
    'start.S',
    'syscallas.S',
)

libos_lds = join_paths(meson.current_source_dir(), 'shim.lds')
