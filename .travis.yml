language: c
dist: bionic
sudo: required
addons:
  apt:
      packages:
        - libssl-dev
        - ninja-build
        - python3
        - python3-click
        - python3-jinja2
        - python3-lxml
        - python3-numpy
        - python3-pip
        - python3-scipy
        - python3-setuptools
script:
  - make -j$(nproc)
    && meson build -Ddirect=enabled
    && ninja -C build &>build.log || tail -n 200 build.log
    && sudo ninja -C build install &>build.lg || tail -n 200 build.log
    && sudo pip3 install pytest
    && export INSTALL_DEPS=1
    && ./run_tests0
    && ./run_tests1
    && ./run_tests2

matrix:
  include:
    - name: Build and test on ppc64 bionic
      os: linux
      arch: ppc64le
      dist: bionic
      env: PYTHONVERSION=python3.6
      before_script:
        - sed -i "s/glibc-2.*/glibc-2.31')/" meson.build
        - python3 -m pip install 'meson>=0.55,<0.56'
    - name: Build and test on ppc64 focal
      os: linux
      arch: ppc64le
      dist: focal
      env: PYTHONVERSION=python3.8
      before_script:
        - sed -i "s/glibc-2.*/glibc-2.33')/" meson.build
        - python3 -m pip install 'meson>=0.55,<0.56'
